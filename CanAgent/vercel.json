{
  "version": 2,
  "builds": [
    { "src": "src/api/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/api/$1.js" }
  ],
  "crons": [
    {
      "path": "/api/run",
      "schedule": "0 9 * * *"
    }
  ]
}
